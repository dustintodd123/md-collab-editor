I wanted the world's simplest collaborative Markdown editor
Ok it's not that simple - version history - I wanted version history 

I deploy on render, and it's setup to use Turso DB if the env variables are set. Otherwise is use file storage. 
- TURSO_URL — the URL from Turso (starts with libsql://)
- TURSO_AUTH_TOKEN — the token f
