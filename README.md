I wanted the world's simplest collaborative Markdown editor

I deploy using render; if the environment variables are set, storage will be handled by Turso, otherwise, file storage.
- TURSO_URL — the URL from Turso (starts with libsql://)
- TURSO_AUTH_TOKEN
